/**
 * HA Better Guest - Configuration Template
 *
 * COPY THIS FILE to: /www/config/ha-better-guest-config.js
 * That location maps to URL: /local/config/ha-better-guest-config.js
 *
 * DO NOT EDIT THIS FILE - it's just a template and HACS will overwrite it!
 */

window.haBetterGuestConfig = {
  // Default settings for all non-admin users
  default: {
    hiddenPanels: [
      'lovelace',        // Default dashboard
      'energy',          // Energy
      'map',             // Map
      'logbook',         // Logbook
      'history',         // History
      'developer-tools', // Developer Tools
      'config',          // Settings
      'media-browser',   // Media
      'todo'             // To-do lists
    ],
    redirectTo: '/profile'
  },

  // Per-user overrides (optional)
  // Example:
  // users: {
  //   'nikky': {
  //     hiddenPanels: ['config', 'developer-tools'],
  //     redirectTo: '/lovelace-home'
  //   }
  // }
  users: {}
};
